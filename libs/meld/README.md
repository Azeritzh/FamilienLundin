# meld

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build meld` to build the library.

## Running unit tests

Run `nx test meld` to execute the unit tests via [Jest](https://jestjs.io).
